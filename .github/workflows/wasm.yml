name: Build Pyodide Wheel

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  build-and-test-wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Pixi
        uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: latest
          cache: true

      - name: Build WebAssembly Wheel
        run: |
          rm -rf build dist mahotas.egg-info .pixi
          pixi run build-wasm

      # STEP MOVED UP: Test while only one wheel exists in /dist
      - name: Test WebAssembly Wheel
        run: pixi run test-wasm

      # STEP MOVED DOWN: Create the duplicate wheel after testing is done
      - name: Generate Forced 4_0_9 Tag
        run: |
          sudo chown -R $USER:$USER dist
          cd dist
          WHEEL=$(ls *.whl | grep "pyemscripten" | head -n 1)
          if [ -n "$WHEEL" ]; then
            FORCED_WHEEL=$(echo $WHEEL | sed 's/pyemscripten_[0-9]\{4\}_[0-9]/emscripten_4_0_9/')
            cp "$WHEEL" "$FORCED_WHEEL"
            echo "Successfully created legacy tagged wheel: $FORCED_WHEEL"
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mahotas-pyodide-wheels-all-tags
          path: dist/*.whl